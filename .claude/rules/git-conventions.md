# Git Conventions

- Branch per phase: feature/<phase-name>
- Commit messages: conventional commits (chore:, feat:, fix:, docs:)
- English commit messages
- No large files (models, audio) in git — use .gitignore
- Squash merge feature branches into main
